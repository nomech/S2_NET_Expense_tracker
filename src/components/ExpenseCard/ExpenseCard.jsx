import React from "react";

const ExpenseCard = ({ expense }) => {
  console.log(expense);

  return (
    <div>
{/*       <p>{expense.title}</p>
      <p>{expense.amount}</p>
      <p>{expense.date}</p>
      <p>{expense.category}</p> */}
    </div>
  );
};

export default ExpenseCard;
